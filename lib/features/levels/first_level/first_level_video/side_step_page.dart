import 'package:flutter/material.dart';

class SideStepVideoPage extends StatelessWidget {
  const SideStepVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
