import 'package:flutter/material.dart';

class DragStepOverVideoPage extends StatelessWidget {
  const DragStepOverVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
