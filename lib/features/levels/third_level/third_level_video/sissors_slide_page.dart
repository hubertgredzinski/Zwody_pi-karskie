import 'package:flutter/material.dart';

class SissorsSlideVideoPage extends StatelessWidget {
  const SissorsSlideVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
