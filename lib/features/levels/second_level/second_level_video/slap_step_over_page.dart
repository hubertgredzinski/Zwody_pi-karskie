import 'package:flutter/material.dart';

class SlapStepOverVideoPage extends StatelessWidget {
  const SlapStepOverVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
